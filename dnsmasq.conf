# interface to bind to
interface=wlan0
# don't read any other file like /etc/resolv.conf for nameservers
no-resolv
# starting_range,end_range,lease_time
dhcp-range=10.0.0.3,10.0.0.20,12h
# dns addresses to send to the clients, defaults to google's
server=8.8.8.8
server=8.8.4.4
